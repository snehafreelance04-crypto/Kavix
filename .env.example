# Frontend -> API host (backend)
VITE_API_URL=https://kavix-be.vercel.app

# Frontend canonical URL (used for OAuth redirect defaults)
VITE_FRONTEND_URL=https://kavix-two.vercel.app
# Optional: if your backend exposes auth routes under a different prefix (eg. '/api/auth') set:
# VITE_AUTH_BASE=/api/auth
